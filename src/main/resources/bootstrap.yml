server:
  port: 8001
spring:
  application:
    name: onechain-onenft
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        refresh-enabled: true
logging:
  level:
    root: info
